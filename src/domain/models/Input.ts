export class Input {
    place: string
    transition: string
    number_of_inputs: number

    constructor(place: string, transition: string, number_of_inputs: number) {
        this.place = place
        this.transition = transition
        this.number_of_inputs = number_of_inputs
    }

}